# fastlane/Appfile — identity for all lanes (CI + deployment).
# Deployment lanes are imported via `import "../deployment/Fastfile"` in Fastfile.
# Deployment-specific Appfile (deployment/Appfile) is not used since --fastlane-dir
# is not a valid Fastlane CLI flag; all config is loaded here instead.
app_identifier("org.mifos.kmp.template")
json_key_file("secrets/android/play/service-account.json")
